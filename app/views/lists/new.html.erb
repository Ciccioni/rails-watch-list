<h3>Create a new list</h3>
  <%= simple_form_for @list do |f| %>
    <%= f.input :name, placeholder: 'Drama' %>
    <%= f.submit class: 'btn btn-flat' %>
  <% end %>

  <%= link_to 'Back', lists_path %> | 
  <%= link_to 'Add a movie', new_list_movie_path %>